language: python
python: "3.4"
# command to install dependencies
install:
- "pip install -r requirements.txt"
# command to run tests
script:
  - py.test -v tests/test_test.py
  - coverage run coverage run pyPodcastParser/__init__.py
  - coverage combine
after_success:
  - coveralls
