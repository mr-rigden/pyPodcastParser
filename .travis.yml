language: python
python: "3.4"
# command to install dependencies
install:
- "pip install -r requirements.txt"
# command to run tests
script:
  - py.test -v tests/test_pyPodcastParser.py
  - coverage run --source pyPodcastParser -m py.test
  - coverage report
after_success:
  coveralls
